# Set the real production origin before building for deployment.
# Without this value the site intentionally uses noindex.
SITE_URL=
# Optional Google Search Console ownership verification token.
GOOGLE_SITE_VERIFICATION=
